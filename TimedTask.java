public class TimedTask extends Task {

    public TimedTask(int id, int userId, String title, Deadline deadline,
    Priority priority, String note) {

        super(id, userId, title, deadline, priority, note);
    }
}
